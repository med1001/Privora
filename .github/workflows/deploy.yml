name: Deploy Backend to EC2

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Backend
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/prvra:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~
            # 1. Créer le dossier secrets s'il n'existe pas
            mkdir -p ~/secrets
            
            # 2. Injecter le JSON de Firebase depuis les secrets GitHub
            cat << 'EOF' > ~/secrets/firebase-adminsdk.json
            ${{ secrets.FIREBASE_ADMIN_JSON_CONTENT }}
            EOF
            
            # 3. Récupérer la dernière image
            docker pull ${{ secrets.DOCKER_USERNAME }}/prvra:latest
            
            # 4. Relancer le backend avec la V2
            docker compose up -d --force-recreate backend
            
            # 5. Nettoyage des anciennes images
            docker image prune -f
